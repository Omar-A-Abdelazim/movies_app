import 'package:flutter/material.dart';

class SearshTap extends StatelessWidget {
  const SearshTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      child: Center(child: Text("SearshTap")),
    );
  }
}
