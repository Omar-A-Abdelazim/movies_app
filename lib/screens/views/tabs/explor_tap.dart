import 'package:flutter/material.dart';

class ExplorTap extends StatelessWidget {
  const ExplorTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Center(child: Text("ExplorTap")),
    );
  }
}
